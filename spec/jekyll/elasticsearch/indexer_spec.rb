require 'spec_helper'

describe Jekyll::Elasticsearch::Indexer do
  it 'has a version number' do
    expect(Jekyll::Elasticsearch::Indexer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
